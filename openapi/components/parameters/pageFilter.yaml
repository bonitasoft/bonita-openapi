description: >-
  can filter on attributes with the format f={filter\_name}={filter\_value} with the name/value pair as url encoded string.
explode: true
in: query
name: f
required: false
schema:
  type: array
  items:
    type: string
    maxLength: 250
    pattern: '^[A-Za-z0-9%]{0,250}$'
style: form
example: 'abc%3d123'
