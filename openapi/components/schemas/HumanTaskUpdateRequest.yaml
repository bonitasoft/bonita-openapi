type: object
properties:
   assigned_id:
     description: The id of the user to assign this Human task to
     type: string
   state:
     description: state of the HumanTask
     type: string
example:
   assigned_id: "1234"
   state: "new_state"
