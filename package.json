{
  "name": "bonita-web-api",
  "version": "0.0.1",
  "description": "Open API Specification for Bonita Platform.",
  "scripts": {
    "start": "redocly preview-docs openapi/openapi.yaml",
    "test": "redocly lint",
    "clean": "rm -rf dist/* && copyfiles -u 2 docs/images/* dist/images/",
    "yaml": "redocly bundle -o dist/openapi.yaml -f",
    "build": "npm run clean && npm run yaml",
    "postman": "npm run build && node_modules/.bin/openapi2postmanv2 -s dist/openapi.yaml -o dist/postman.json -p",
    "static": "redoc-cli build ./dist/openapi.yaml -t docs/index.html.hbs --cdn -o ./dist/index.html",
    "package": "npm test && npm run postman && npm run static"
  },
  "license": "GPL v2.0",
  "private": true,
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "openapi-to-postmanv2": "^4.3.0",
    "@redocly/cli": "^1.0.0-beta.112"
  },
  "dependencies": {
    "redoc-cli": "^0.13.20"
  }
}
